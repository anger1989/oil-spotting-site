# Oil Spotting - Официальный сайт группы

Современный веб-сайт для музыкальной группы Oil Spotting в жанрах synthwave, deep house, rock и cosmic space.

## 🚀 Технологический стек

- **Next.js 14** - React фреймворк для продакшена
- **TypeScript** - Типобезопасность
- **Tailwind CSS** - Современная стилизация
- **Framer Motion** - Плавные анимации
- **Docker** - Контейнеризация приложения

## 📦 Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Откройте http://localhost:3000 в браузере
```

### Сборка для продакшена

```bash
# Сборка проекта
npm run build

# Запуск продакшен сервера
npm start
```

### Запуск в Docker

#### С docker-compose (рекомендуется)

```bash
# Создайте .env файл с переменными окружения (или используйте значения по умолчанию)
# NEXT_PUBLIC_GA_ID=G-47S7M2240C
# NEXT_PUBLIC_SITE_URL=https://oilspotting.com

# Сборка и запуск
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

#### Ручная сборка Docker образа

```bash
# Сборка с передачей переменных окружения
docker build \
  --build-arg NEXT_PUBLIC_GA_ID=G-47S7M2240C \
  --build-arg NEXT_PUBLIC_SITE_URL=https://oilspotting.com \
  -t oil-spotting .

# Запуск контейнера
docker run -p 3000:3000 oil-spotting
```

**Важно:** Переменные `NEXT_PUBLIC_*` должны быть переданы на этапе сборки (build time), так как они встраиваются в клиентский код Next.js.

## 🎨 Особенности

- ✨ Современный космический дизайн
- 🎭 Плавные анимации и эффекты
- 📱 Полностью адаптивный дизайн
- 🌌 Интерактивные элементы с эффектом параллакса
- 🎵 Секция со ссылками на стриминговые сервисы
- 👥 Информация об участниках группы

## 📁 Структура проекта

```
oil-spotting/
├── app/                 # Next.js App Router
│   ├── layout.tsx      # Корневой layout
│   ├── page.tsx        # Главная страница
│   └── globals.css     # Глобальные стили
├── components/          # React компоненты
│   ├── Navigation.tsx  # Навигация
│   ├── Hero.tsx        # Главный баннер
│   ├── About.tsx       # О группе
│   ├── Members.tsx     # Участники
│   └── Streaming.tsx   # Стриминговые сервисы
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
└── package.json        # Зависимости проекта
```

## 🎯 Настройка

### Переменные окружения

Создайте файл `.env.local` в корне проекта (см. `.env.example`):

```bash
NEXT_PUBLIC_GA_ID=G-47S7M2240C
NEXT_PUBLIC_SITE_URL=https://oilspotting.com
```

**Для Docker:** Переменные окружения передаются через build args при сборке. В `docker-compose.yml` используются значения по умолчанию, но вы можете переопределить их через `.env` файл или переменные окружения системы.

### Обновление информации о группе

Отредактируйте компоненты в папке `components/`:
- `About.tsx` - описание группы
- `Members.tsx` - список участников
- `Streaming.tsx` - ссылки на стриминговые сервисы

### Изменение цветовой схемы

Настройте цвета в `tailwind.config.ts` в секции `colors.cosmic`.

### Работа с изображениями

Изображения должны находиться в папке `public/images/`. Текущие изображения используются:
- `openart-image_q2x8A3u4_1762805953702_raw.png` - фоновое изображение в Hero и Streaming секциях
- `openart-image_S9OAD-dS_1762711038080_raw.png` - изображение в секции "О группе"

Для замены изображений просто замените файлы в `public/images/` с теми же именами или обновите пути в компонентах.

## 📝 Лицензия

Все права защищены © Oil Spotting

